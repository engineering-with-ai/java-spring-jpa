<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>com.ewa</groupId>
  <artifactId>java-spring-jpa</artifactId>
  <version>1.0.0-beta</version>
  <name>java-spring-jpa</name>
  <description>Domain-neutral Spring Boot + Spring Data JPA service template</description>

  <properties>
    <java.version>21</java.version>
    <!-- Static-analysis / format tool versions (Boot parent manages the rest) -->
    <springdoc.version>3.1.1</springdoc.version>
    <wiremock.version>3.13.1</wiremock.version>
    <spotless.version>3.10.2</spotless.version>
    <spotbugs.version>4.10.4.1</spotbugs.version>
    <jacoco.version>0.8.15</jacoco.version>
    <jacoco.line.min>0.70</jacoco.line.min>
    <!-- Named-verb layer tool versions (the poe / npm-script analog) -->
    <build-helper.version>3.6.1</build-helper.version>
    <pmd.version>3.28.0</pmd.version>
    <dependency-check.version>13.0.0</dependency-check.version>
  </properties>

  <dependencies>
    <!-- web + persistence + validation: the TypeORM/Tortoise + zod/Pydantic analogs -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <!-- hot-reload parity with nest's start:dev watch mode / uvicorn's reload flag. optional
         plus runtime scope so it never leaks into anything that depends on this jar (Spring
         Initializr's own convention). -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-devtools</artifactId>
      <scope>runtime</scope>
      <optional>true</optional>
    </dependency>
    <!-- auto-configured RestClient.Builder for the outbound call (Boot 4 split this out) -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-restclient</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- OpenAPI: the @nestjs/swagger analog -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <!-- nullness contracts (records annotated @NullMarked at package level) -->
    <dependency>
      <groupId>org.jspecify</groupId>
      <artifactId>jspecify</artifactId>
      <version>1.0.1</version>
    </dependency>

    <!-- test: JUnit 5, Mockito, AssertJ, Spring Test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- @ServiceConnection wiring + real Postgres for *IT (testcontainer analog) -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- mocks the outbound call: the nock / pook analog. -standalone bundles its own HTTP server. -->
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-standalone</artifactId>
      <version>${wiremock.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Force JDK 21 for compile/test even when `mvn` itself runs on another JDK.
           Reads ~/.m2/toolchains.xml; Gradle / other tools never consult it. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-toolchains-plugin</artifactId>
        <version>3.3.0</version>
        <configuration>
          <toolchains>
            <jdk>
              <version>21</version>
            </jdk>
          </toolchains>
        </configuration>
        <executions>
          <execution>
            <goals><goal>toolchain</goal></goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <!-- dep hygiene: the depcheck / deptry analog. Advisory — reports unused *declared*
           deps at `verify`; does not fail the build (starter transitives make it noisy). -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <configuration>
          <ignoreNonCompile>true</ignoreNonCompile>
          <failOnWarning>false</failOnWarning>
        </configuration>
        <executions>
          <execution>
            <id>analyze</id>
            <phase>verify</phase>
            <goals><goal>analyze-only</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- adds tests/java as a second test-source root, sibling to the default src/test/java.
           *Test (unit) stays in src/test/java; *IT (integration) lives in tests/java — matching
           the python/typescript siblings' separate tests/ folder. Reason: testSourceDirectory
           is a single-value POM element and would replace src/test/java wholesale, so this has
           to be an *additional* root via build-helper, not a plain POM setting. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>${build-helper.version}</version>
        <executions>
          <execution>
            <id>add-integration-test-source</id>
            <phase>generate-test-sources</phase>
            <goals><goal>add-test-source</goal></goals>
            <configuration>
              <sources>
                <source>tests/java</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- format: the prettier / black analog. `spotless:apply` writes, `spotless:check` gates.
           Reason: spotless hardcodes its own default target globs and does NOT follow extra test
           source roots added via build-helper (diffplug/spotless#1214) — tests/java has to be
           listed explicitly or it silently stops being formatted/checked. -->
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.version}</version>
        <configuration>
          <java>
            <includes>
              <include>src/main/java/**/*.java</include>
              <include>src/test/java/**/*.java</include>
              <include>tests/java/**/*.java</include>
            </includes>
            <googleJavaFormat/>
            <importOrder/>
            <removeUnusedImports/>
            <formatAnnotations/>
          </java>
        </configuration>
        <executions>
          <execution>
            <goals><goal>check</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- bug-pattern analysis (bytecode-level) — bundled under the `audit-src` verb, not `lint`.
           Bound to `verify`. includeTests=false already, so the tests/java move needs no change
           here: it never scanned test classes in the first place. -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${spotbugs.version}</version>
        <configuration>
          <includeTests>false</includeTests>
          <excludeFilterFile>spotbugs-exclude.xml</excludeFilterFile>
        </configuration>
        <executions>
          <execution>
            <goals><goal>check</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- lint: the ruff / eslint correctness+style analog (SpotBugs is bytecode bug-pattern
           detection, a different category — that stays under audit-src). Bound to `verify`, like
           SpotBugs. includeTests=true mirrors the siblings' `eslint src tests`. testSourceDirectories
           defaults to project.testCompileSourceRoots (since 3.7), so this DOES follow the
           build-helper-added tests/java root automatically — no extra wiring needed here. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <version>${pmd.version}</version>
        <configuration>
          <targetJdk>${java.version}</targetJdk>
          <includeTests>true</includeTests>
          <rulesets>
            <ruleset>pmd-ruleset.xml</ruleset>
          </rulesets>
        </configuration>
        <executions>
          <execution>
            <goals><goal>check</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- audit-packages: the pip-audit / npm-audit analog. NOT bound to any phase — deliberately
           advisory-only, invoked solely via the `audit-packages` Makefile target.
           Reason: without a free NVD API key the first scan can take a long time against NVD's
           public rate limit, and false positives are common — gating `mvn verify` on this by
           default would make every fresh clone's first build slow/flaky before anyone has tuned
           suppressions. nvdApiKeyEnvironmentVariable (not nvdApiKey/-DnvdApiKey) reads the key
           straight from the NVD_API_KEY env var at runtime — the plugin's own docs call this out
           as the safer option, since a -D flag or an interpolated ${env.*} property can leak into
           Maven debug logs. See template-secrets.env for NVD_API_KEY. failBuildOnCVSS=7 only
           matters when you actually invoke this verb — fails on High/Critical, not Medium/Low,
           same bar as the nestjs sibling's npm audit moderate threshold. -->
      <plugin>
        <groupId>org.owasp</groupId>
        <artifactId>dependency-check-maven</artifactId>
        <version>${dependency-check.version}</version>
        <configuration>
          <failBuildOnCVSS>7</failBuildOnCVSS>
          <nvdApiKeyEnvironmentVariable>NVD_API_KEY</nvdApiKeyEnvironmentVariable>
        </configuration>
      </plugin>

      <!-- integration tests: *IT via Failsafe, bound to `verify` (unit *Test stays on Surefire).
           Scans target/test-classes by class name, not by source dir, so the tests/java move
           needs no config change here either. jdkToolchain: maven-toolchains-plugin's own
           `toolchain` goal is bound to `validate`, which only runs as part of the full lifecycle
           — a bare `mvnw failsafe:integration-test` (single-goal CLI syntax, what the `integration`
           Makefile target calls) skips `validate` entirely, so without this Failsafe forks its
           test JVM under whatever JDK ran Maven itself, not JDK 21, and chokes on the JDK21-
           compiled *IT classes (class file version mismatch). jdkToolchain makes Failsafe resolve
           the toolchain itself, independent of whether the toolchains-plugin goal already ran. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <jdkToolchain>
            <version>${java.version}</version>
          </jdkToolchain>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- coverage: the c8 / pytest-cov analog. <rules> lives at the PLUGIN level (not nested
           under the `check` execution) so a bare `mvnw jacoco:check` CLI call (what `make cover`
           does) picks it up too — a bare goal invocation runs as a synthetic "default-cli"
           execution, which only inherits plugin-level config, never config nested inside a named
           execution (same class of gotcha as maven-failsafe-plugin's jdkToolchain above). -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <configuration>
          <rules>
            <rule>
              <element>BUNDLE</element>
              <limits>
                <limit>
                  <counter>LINE</counter>
                  <value>COVEREDRATIO</value>
                  <minimum>${jacoco.line.min}</minimum>
                </limit>
              </limits>
            </rule>
          </rules>
        </configuration>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals><goal>prepare-agent</goal></goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals><goal>report</goal></goals>
          </execution>
          <execution>
            <id>check</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
